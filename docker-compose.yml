---
version: "3.9"
services:
  web:
    image: main/backend
    build:
      context: ./docker-images
      dockerfile: python3.11.1-slim.dockerfile
    env_file:
      - '_env/prod.env'
    ports:
      - "8000:8000"
    volumes:
      - './data:/data'
